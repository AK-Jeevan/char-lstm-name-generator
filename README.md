# char-lstm-name-generator
A language-conditioned character-level LSTM model that generates realistic names based on training data per language. Includes custom character embeddings (Word2Vec), language embeddings, and temperature-based sampling.
